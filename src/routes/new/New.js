import React from 'react';

import Layout from 'shared/components/Layout';
import { PAGE_NAME } from 'shared/constants/PageName';

const New = () => {

    return (
        <Layout pageName={PAGE_NAME.NEW}>
            New
        </Layout>
    );
}

export default New;